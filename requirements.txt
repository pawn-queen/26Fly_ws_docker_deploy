# Intentionally installed with --no-deps.
# torch, torchvision and ultralytics belong to the pinned Jetson base image.
# Keep the image on NumPy 1.x for ROS Humble cv_bridge while satisfying the
# TensorFlow 2.19 dependency already present in the pinned Jetson base image.
# OpenCV is held at the final NumPy-1-compatible headless wheel.
# colcon-core 0.21.x rejects setuptools 80 and newer.
numpy==1.26.4
scipy==1.10.1
opencv-python-headless==4.11.0.86
lap==0.5.12
setuptools>=30.3.0,<80
