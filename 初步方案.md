本项目面向 NVIDIA Jetson Orin Nano Super 8GB 实机无人机平台，运行：
-  Ubuntu 22.04 
-  JetPack 6 
-  CUDA / TensorRT 
-  ROS 2 Humble 
-  PX4 
-  YOLO视觉推理 
-  RealSense深度相机 
-  实机控制程序 
系统部署目标不是传统服务器场景下的“云原生微服务”，而是：
构建一个长期稳定运行、可快速恢复、可复制部署的机器人计算环境。
机器人系统与普通软件服务最大的区别在于：
1.  软件高度依赖硬件； 
2.  软件环境复杂； 
3.  现场维护成本高； 
4.  设备状态动态变化； 
5.  任务执行具有强人工控制需求。 
因此采用：
Docker负责软件环境封装，systemd负责基础通信服务管理，ROS负责机器人应用运行，宿主机负责硬件与驱动。
这种分层架构符合现代机器人部署思路。许多机器人平台也采用类似的“Linux系统层 + Docker运行环境 + ROS应用层”的分层方式。
整体结构：
```
Jetson Orin Nano Super
│
│
├── Ubuntu 22.04 Host
│
│   ├── NVIDIA Driver
│   ├── JetPack Runtime
│   ├── Kernel
│   ├── USB/UART/Camera Driver
│   ├── udev
│   └── Docker Engine
│
│
└── 26fly-runtime Container
        │
        │
        ├── ROS 2 Humble
        │
        ├── PX4 Communication Layer
        │       │
        │       ├── Micro XRCE DDS Agent
        │       │       (systemd管理)
        │       │
        │       └── mavlink-routerd
        │               (systemd管理)
        │
        │
        ├── px4_msgs
        │       (ROS接口包，不是服务)
        │
        ├── RealSense ROS Node
        │
        ├── detect
        │
        └── control
                (人工启动)
```
本项目不采用：
```
每个ROS节点一个container
```
这种云原生思想。
原因：
机器人系统中：
```
camera
↓
detect
↓
control
↓
PX4
```
高度耦合。
拆分成多个container会增加：
-  DDS发现复杂度 
-  网络问题 
-  volume管理 
-  GPU共享问题 
-  USB设备映射问题 
-  调试成本 
因此采用：
```
一个长期存在runtime container
```
即：
Pet Container（宠物容器）
而不是：
Cattle Container（一次性容器）
传统服务器：
```
container坏了

↓

删除

↓

重新创建
```
因为：
-  镜像固定 
-  状态无所谓 
-  服务可快速恢复 
但是无人机：
```
Docker image
+
ROS workspace
+
colcon build
+
CUDA/TensorRT环境
+
模型
+
硬件配置
```
构建成本高。
特别是：
-  PyTorch 
-  TensorRT 
-  ROS依赖 
-  ARM64编译 
重新部署代价巨大。
因此采用：
```
第一次：

docker build
docker create
初始化


以后：

docker start
docker stop
docker exec
```
容器长期存在。
宿主机只负责：
包括：
```
USB
UART
CSI Camera
GPU Driver
```
因为：
Docker无法替代Kernel。
包括：
```
CUDA Driver
NVIDIA Container Runtime
JetPack
```
GPU硬件访问必须依赖宿主。
宿主：
```
Docker Engine
        |
        |
        v

26fly-runtime
```
Docker容器负责：
包括：
```
ROS2 Humble

Python

PyTorch

TensorRT

Ultralytics

OpenCV

cv_bridge

ROS packages

编译环境
```
保证：
开发环境：
```
WSL Ubuntu
```
和：
实机：
```
Jetson
```
软件一致。
采用：
```
/workspace

├── src
│     |
│     └── 宿主Git挂载
│         read only
│
├── build
│
├── install
│
└── log
```
关系：
```
宿主机

Git源码
    |
    |
    v

Container

/workspace/src
(ro)


build/install/log

(volume)
```
因为容器运行：
```
root
```
如果源码可写：
可能产生：
```
root-owned文件

__pycache__
egg-info
build文件
临时文件
```
污染宿主Git。
所以：
```
源码:
只读


运行:
可写
```
```
root
+
privileged
+
/dev:/dev
```
而不是：
```
普通用户
+
video group
+
dialout group
+
render group
```
原因：
机器人现场更关注：
```
可靠性
```
而不是：
```
权限最小化
```
传统方案：
```
RealSense
需要video


PX4
需要dialout


GPU
需要render


USB
需要plugdev
```
换设备：
```
GID变化

权限失效
```
新方案：
```
container root

↓

直接访问设备
```
减少：
-  用户组维护 
-  USB变化问题 
-  tty变化问题 
采用：
```
/dev:/dev
```
完整映射。
不是：
```
/dev/ttyACM1:/dev/px4
```
原因：
设备编号动态变化：
例如：
今天：
```
ttyACM0
```
明天：
```
ttyACM1
```
单独映射会增加故障。
同时：
不执行：
```
chmod -R 777 /dev
```
原因：
/dev是宿主设备树。
修改：
```
container:
/dev/video0
```
实际上修改：
```
host:
/dev/video0
```
没有必要。
systemd不负责所有ROS节点。
只负责：
原因：
它属于基础通信设施。
链路：
```
PX4 FMU

↓

UART/USB

↓

Micro XRCE DDS Agent

↓

DDS

↓

ROS2
```
它：
-  开机启动 
-  长期运行 
-  失败自动恢复 
适合systemd。
同理：
```
PX4

↓

MAVLink

↓

mavlink-routerd

↓

QGroundControl/GCS
```
属于通信基础设施。
适合：
```
mavlink-routerd.service
```
control属于：
```
任务应用层
```
特点：
-  不常驻 
-  参数经常变化 
-  比赛人工选择 
-  执行完成退出 
因此：
不设计：
```
control.service enabled
```
避免：
```
Jetson开机

↓

自动启动飞控逻辑
```
这种危险行为。
control启动方式：
例如：
```
比赛A:

./fly_A.sh


↓

docker exec runtime

↓

启动control
```
任务结束：
```
control退出
```
基础服务继续运行。
px4_msgs不是服务。
它是：
```
ROS2 interface package
```
作用：
提供：
```
VehicleStatus.msg

VehicleLocalPosition.msg

VehicleCommand.msg
```
等接口。
生命周期：
```
colcon build
```
产生：
```
ROS message type support
```
供：
```
control
```
使用。
不存在：
```
systemctl start px4_msgs
```
固定：
```
ROS2 Humble
```
通信：
```
rmw_fastrtps_cpp
```
Micro XRCE DDS Agent选择与Humble兼容版本。
目标：
```
ROS2 Humble

↓

Fast DDS

↓

Micro XRCE DDS Agent
```
保持同一DDS生态。
不采用：
```
ROS2一套FastDDS

Agent另一套FastDDS
```
避免动态库冲突。
```
Ubuntu启动

↓

Docker启动

↓

runtime container启动

↓

systemd启动

↓

Micro XRCE Agent启动

mavlink-routerd启动
```
检查：
```
systemctl status micro-xrce-agent

systemctl status mavlink-routerd
```
确认：
```
PX4通信链路正常
```
人工选择：
```
fly_A.sh
fly_B.sh
fly_C.sh
```
启动：
```
control
```
执行：
```
起飞
搜索
识别
控制
降落
```
结束：
```
control退出
```
systemd：
```
Restart=always
```
自动恢复。
systemd：
```
Restart=always
```
自动恢复。
不自动拉起。
原因：
任务程序错误需要人工分析。
```
================================================

任务层

control
比赛逻辑
飞行策略


================================================

感知层

detect
YOLO
RealSense


================================================

机器人通信层

ROS2 Humble

px4_msgs


================================================

系统通信基础设施

Micro XRCE DDS Agent
mavlink-routerd


================================================

运行环境

Docker

CUDA

TensorRT


================================================

硬件层

Ubuntu
Kernel
JetPack
NVIDIA Driver


================================================
```
最终采用：
```
一个长期存在的Docker宠物容器

        +

root + privileged + /dev:/dev

        +

ROS2 Humble固定环境

        +

systemd只管理：

        Micro XRCE DDS Agent
        mavlink-routerd

        +

control人工启动

        +

源码只读

        +

build/install/log独立volume

        +

动态硬件状态由应用层处理
```
核心思想：
Docker解决“软件环境一致性”，systemd解决“基础通信服务可靠性”，ROS解决“机器人应用逻辑”，人工脚本控制“任务生命周期”。
该架构牺牲部分Docker最佳实践中的隔离性，但换取了无人机实机比赛最重要的指标：
可复现、可维护、可快速恢复、低现场故障率。
这更接近实际机器人产品部署，而不是普通服务器应用部署。